# GitLab groups to clone all repositories from
# One group URL per line, comments starting with # are ignored

# Add your GitLab group URLs here, one per line
# The GitLab host is derived automatically from these URLs
# Example:
# https://your-gitlab.example.com/your-group
# https://your-gitlab.example.com/another-group
